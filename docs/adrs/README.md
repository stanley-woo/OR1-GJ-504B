# ADRs

Use this folder for short architecture decision records.

Suggested first ADRs:

- why the project starts as a modular monolith
- why Next.js was chosen over Flutter for `v1`
- which market data provider was chosen and why
- why snapshots are handled by a separate worker

